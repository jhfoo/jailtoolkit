name: dhcp-deadend
release: {{jailrelease}}
props:
  vnet: true
  dhcp: false
  bpf: true
  template: false
  interfaces: vnet0:jailprivate
pkgs:
  - git
tasks:
  - name: install dnsmasq
    do: runtemplate
    template: dnsmasq
  - name: restart jail
    do: jailrestart